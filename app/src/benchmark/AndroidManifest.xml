<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name="tv.mars.app.benchmark.ForceGcReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="tv.mars.app.benchmark.FORCE_GC" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
